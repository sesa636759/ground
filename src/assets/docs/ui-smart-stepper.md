# ui-smart-stepper

## Properties

| Property | Type | Description |
| --- | --- | --- |
| **orientation** | `'horizontal' \| 'vertical'` | Orientation of the stepper (horizontal or vertical) |
| **size** | `'sm' \| 'md' \| 'lg'` | Size of the stepper |
| **activeStep** | `number` | Current active step index (0-based) |
| **host** | `HTMLElement` | Reference to the host element |
| **index** | `number` |  |
| **step** | `HTMLElement` |  |

## Events

No events found.

## Usage Example

```html
<ui-smart-stepper
  [orientation]="value"
  [size]="value"
  [activeStep]="value"
></ui-smart-stepper>
```
